# 🚀 دليل تشغيل المشروع بكل سهولة

تم تشغيل المشروع حالياً وهو يعمل على الرابط التالي:
👉 **[http://localhost:5173](http://localhost:5173)**

---

## ⚡ الطريقة الأولى: التشغيل بضغطة زر واحدة (الأسهل)
في المجلد الرئيسي للمشروع، ستجد ملفاً اسمه:
📁 **`تشغيل_المشروع.bat`**

- فقط اضغط عليه مرتين (**Double Click**).
- سيقوم تلقائياً بفتح شاشة التشغيل وفتح المتصفح على النظام مباشرة.

---

## 💻 الطريقة الثانية: التشغيل اليدوي (عبر سطر الأوامر / Terminal)

إذا أردت تشغيله يدوياً في أي وقت، اتبع الـ 3 خطوات البسيطة التالية:

### الخطوة 1: فتح سطر الأوامر داخل مجلد المشروع
افتح `Terminal` أو `PowerShell` أو `CMD` وانتقل لمجلد `project`:
```bash
cd project
```

### الخطوة 2: تثبيت الحزم (تُجرى لمرة واحدة فقط إذا لم تكن مثبتة)
```bash
npm install
```
*(ملاحظة: الحزم مثبتة بالفعل وجاهزة حالياً).*

### الخطوة 3: تشغيل السيرفر المحلي
```bash
npm run dev
```

ستظهر لك رسالة تفيد بأن السيرفر يعمل:
```text
  VITE v5.4.8  ready in ... ms

  ➜  Local:   http://localhost:5173/
```
افتح الرابط في متصفحك: **[http://localhost:5173](http://localhost:5173)**

---

## 🔑 بيانات تسجيل الدخول التجريبية (Demo Accounts)

النظام يحتوي على دورين مجهزين للتجربة:

| الدور (Role) | اسم المستخدم (Username) | كلمة المرور (Password) | الصلاحيات والصفحات |
| :--- | :--- | :--- | :--- |
| **مسؤول النظام (Admin)** | `admin` | أي كلمة مرور (أو اضغط دخول مباشرة) | إدارة الكاميرات، المناطق، المستخدمين، سجلات التدقيق، إعدادات الإشعارات |
| **المشرف الميداني (Supervisor)** | `supervisor` | أي كلمة مرور (أو اضغط دخول مباشرة) | البث المباشر للكاميرات، رصد الحوادث والتنبيهات، مراجعة وأرشفة الحرائق |

---

## 🛑 إيقاف التشغيل
لإيقاف السيرفر في أي وقت:
- اضغط على `Ctrl + C` داخل شاشة الأوامر، ثم اكتب `y` واضغط `Enter`.
