# 📋 خطة تنفيذ ومراجعة واجهات البروتوتايب (Prototype Task Plan)

---

## 🎯 المنهجية وآلية العمل المشتركة:
1. نمر على الواجهات **واجهة بواجهة** بالترتيب الهندسي المعتمد.
2. يتم بناء/تجهيز وتدقيق مكونات الواجهة بالكامل (عناصر بصرية، نماذج، جداول، أزرار، وتفاعلات تجريبية).
3. أطلب منك مراجعة الواجهة والتأكد من تطابقها مع البنود والمتطلبات.
4. عندما تتأكد وتقول لي **"تم"**، أقوم بالتأشير عليها كمنجزة `[x]` والانتقال فوراً للواجهة التالية.

---

## 📌 قائمة المهام والواجهات (Interface Tasks Tracker)

### [x] المهمة 1: تسجيل الدخول (`UI-AUTH-01`)
* **الاسم في النظام:** `تسجيل الدخول` (`Login`)
* **الأدوار:** مشترك (مدير النظام Admin / مشرف العمليات Supervisor)
* **المراجع:** متطلب `3.4.14`، مرحلة `1`، مخططات `Picture 1, 2, 3, 5`
* **الحالة:** ✅ تم الاعتماد والإنجاز بنجاح
* **بنود المراجعة والتحقق (Checklist):**
  - [x] حقل إدخال اسم المستخدم / البريد الإلكتروني.
  - [x] حقل إدخال كلمة المرور مع زر إظهار/إخفاء كلمة المرور التفاعلي (أيقونة العين).
  - [x] زر "تسجيل الدخول".
  - [x] ظهور رسالة خطأ عند كتابة بيانات غير صحيحة (`عرض رسالة خطاء`).
  - [x] التوجيه التلقائي للمشرف نحو `الكاميرات والمناطق`، وللمدير نحو `إدارة الكاميرات وتحديد ROI`.
  - [x] إخفاء الصفحات والأقسام المحجوبة عن المشرف بالكامل من القائمة الجانبية ومنع الوصول إليها.
  - [x] زر تسجيل الخروج مع العودة لواجهة الدخول.

---

### [x] المهمة 2: الكاميرات والمناطق (`UI-MONITOR-02`)
* **الاسم في النظام:** `الكاميرات والمناطق` (`Live Monitoring`)
* **الأدوار:** مشترك (مشرف العمليات Supervisor / مدير النظام Admin)
* **المراجع:** متطلبات `3.4.2, 3.4.3, 3.4.9`، مراحل `3, 7`، مخططات `Picture 2, 4`
* **الحالة:** ✅ تم الاعتماد والإنجاز بنجاح
* **بنود المراجعة والتحقق (Checklist):**
  - [x] رأس صفحة مبسط ومباشر مع شارات إحصائية سريعة (4 متصلة، 1 منقطعة، 1 تنبيه نشط).
  - [x] شريط تنبيه انقطاع الكاميرا التلقائي البارز (`عرض تنبيه انقطاع الكاميرا` لكاميرا-05 حسب Picture 4).
  - [x] شريط تصفية خفيف وسريع للكاميرات (الكل، المتصلة، التنبيهات، المنقطعة) مع أزرار تفعيل/إلغاء مربعات الذكاء الاصطناعي وROI.
  - [x] شبكة الكاميرات (5 كاميرات) مع **تمييز بصري فائق الوضوح للحالة اللحظية**:
    * 🟢 **شارة متصلة (Online)** خضراء واضحة على الكاميرات المتصلة (كاميرا 1، 3، 4).
    * 🚨 **شارة تنبيه حريق!** بإطار أحمر متوهج وصندوق رصد الدخان مع زر مباشر "مراجعة الحدث" (كاميرا 2).
    * 🔴 **شارة منقطعة (Offline)** حمراء بارزة مع شاشة توقف البث وتنبيه فقدان الإشارة (كاميرا 5).
  - [x] زر **إعادة الاتصال بالبث يدوياً** عند انقطاع الكاميرا (متاح في بطاقة الكاميرا المنقطعة وفي شريط التنبيه العلوي مع تأثير استعادة الاتصال).
  - [x] مخطط مبسط وأنيق أسفل الكاميرات يوضح توزيع مناطق المستودع الـ 5 مع حالة كل منطقة.

---

### [x] المهمة 3: مراجعة الحدث (`UI-INCIDENT-03`)
* **الاسم في النظام:** `مراجعة الحدث` (`Incident Details & Event Review`)
* **الأدوار:** مشرف العمليات (Supervisor) ومدير النظام (Admin)
* **المراجع:** متطلبات `3.4.8, 3.4.10`، والواجهة رقم 6 في `interfaces.txt`
* **الحالة:** ✅ تم الاعتماد والإنجاز بنجاح
* **بنود المراجعة والتحقق (Checklist):**
  - [x] **عرض تفاصيل الحادثة:** (رقم البلاغ `بلاغ-2041`، الكاميرا `كاميرا-02`، الموقع `المنطقة ب - ممر 04`، وقت الرصد، ومستوى الخطورة `خطر مرتفع 87%`).
  - [x] **عرض صورة الدليل:** صورة الكاميرا مع مربع رصد الدخان بالذكاء الاصطناعي (AI Bounding Box) ونسبة الثقة (`دخان 94%`).
  - [x] **التحقق من استمرارية الحركة:** أزرار شريط الإطارات المتتابعة (قبل/بعد) لملاحظة تصاعد الدخان (متطلب 3.4.6).
  - [x] **زر تأكيد الحريق (الإقرار بالحادثة):** لتأكيد البلاغ وتفعيل الاستجابة وتحديث حالته إلى "مؤكد".
  - [x] **زر تصنيف كإنذار خاطئ:** مع نافذة منبثقة لكتابة واختيار **سبب الإنذار الخاطئ** وحفظه ضمن السجل (متطلب 3.4.10).
  - [x] **زر توجيه دورية أمن وسلامة:** للتفتيش والمعاينة الميدانية في موقع الحادثة.
  - [x] **إشعار النجاح والتوثيق:** ظهور رسالة نجاح الإجراء وتوثيق قرار المشرف في سجل العمليات والتدقيق الأمني.

---

### [x] المهمة 4: التقارير ومؤشرات الأداء / سجل التنبيهات (`UI-ARCHIVE-04`)
* **الاسم في النظام:** `التقارير ومؤشرات الأداء` / `سجل التنبيهات` (`Reports & Incident Archive`)
* **الأدوار:** مشترك (مشرف العمليات Supervisor / مدير النظام Admin)
* **المراجع:** متطلب `3.4.11`، والواجهة رقم 8 في `interfaces.txt`، ومخططات `Picture 2, 5`
* **الحالة:** ✅ تم الاعتماد والإنجاز بنجاح
* **بنود المراجعة والتحقق (Checklist):**
  - [x] **شريط البحث والتصفية المتقدم:** إمكانية التصفية حسب (الكاميرا، التاريخ والوقت، مستوى الخطورة، والحالة: قيد التحقق/مؤكدة/إنذار خاطئ/معالجة).
  - [x] **جدول سجل الحوادث والأرشيف:** عرض الحوادث السابقة (رقم البلاغ، الوقت، العنوان، الكاميرا، نسبة الثقة، الحالة، الإجراء).
  - [x] **معاينة تفاصيل الحادثة المؤرشفة والأدلة:** نافذة منبثقة (Modal) لمعاينة صورة الدليل، والسبب المسجل، وبيانات الإغلاق.
  - [x] **أزرار تصدير التقارير:** تصدير السجل بصيغة CSV / PDF مع إشعار النجاح.
  - [x] **مؤشرات الأداء والإحصائيات:** بطاقات إحصائية واضحة (إجمالي الحوادث، معدل الاستجابة، الإنذارات الخاطئة، نسبة الدقة).

---

### [ ] المهمة 5: إدارة الكاميرات وتحديد ROI (`UI-CAMERA-05`)
* **الاسم في النظام:** `إدارة الكاميرات وتحديد ROI` (`Camera Management`)
* **الأدوار:** مدير النظام (Admin) حصرياً
* **المراجع:** متطلبات `3.4.1, 3.4.3`، مرحلة `2`، مخططات `Picture 1, 3`
* **الحالة:** ⏳ قيد المراجعة والتدقيق
* **بنود المراجعة والتحقق (Checklist):**
  - [ ] جدول الكاميرات المسجلة مع بيانات الاتصال والحالة الحالية (متصلة / منقطعة).
  - [ ] نموذج إضافة كاميرا جديدة (اسم الكاميرا، رابط البث RTSP / مسار ملف الفيديو، المنفذ، الموقع).
  - [ ] خيار تعديل بيانات كاميرا مسجلة.
  - [ ] زر حذف كاميرا مع رسالة تأكيد تحذيرية.
  - [ ] زر الانتقال لضبط منطقة الاهتمام (ROI) لكل كاميرا.
  - [ ] زر حفظ بيانات الكاميرا وتحديث حالتها.

---

### [ ] المهمة 6: تحديد منطقة الاهتمام (ROI) (`UI-ROI-06`)
* **الاسم في النظام:** `تحديد منطقة الاهتمام (ROI)` (`ROI Configuration`)
* **الأدوار:** مدير النظام (Admin) حصرياً
* **المراجع:** متطلب `3.4.4`، مرحلة `2`، مخططات `Picture 1, 3, 4`
* **الحالة:** ⏸️ معلقة
* **بنود المراجعة والتحقق (Checklist):**
  - [ ] لوحة الرسم (Canvas) التفاعلية على لقطة الكاميرا الحية.
  - [ ] إمكانية رسم مضلع (Polygon) لتحديد نطاق المراقبة وعزل النوافذ والمصابيح.
  - [ ] أدوات التحكم (رسم جديد، مسح، معاينة التظليل العكسي).
  - [ ] زر "حفظ منطقة الاهتمام" وزر "إلغاء".

---

### [ ] المهمة 7: إدارة المستخدمين والصلاحيات (`UI-USER-07`)
* **الاسم في النظام:** `إدارة المستخدمين والصلاحيات` (`Users & Permissions`)
* **الأدوار:** مدير النظام (Admin) حصرياً
* **المراجع:** متطلب `3.4.14`، مرحلة `1`، مخطط `Picture 1`
* **الحالة:** ⏸️ معلقة
* **بنود المراجعة والتحقق (Checklist):**
  - [ ] جدول عرض المستخدمين المسجلين وأدوارهم (Admin / Supervisor).
  - [ ] نموذج إضافة مستخدم جديد (الاسم، اسم الدخول، كلمة المرور، تحديد الدور، رقم الهاتف، معرف تيليجرام).
  - [ ] خيار تعديل بيانات المستخدم أو إعادة تعيين كلمة المرور.
  - [ ] خيار حذف / تعطيل حساب مستخدم.

---

### [ ] المهمة 8: قنوات التنبيه والإشعارات (`UI-NOTIF-08`)
* **الاسم في النظام:** `قنوات التنبيه والإشعارات` (`Notification Channels`)
* **الأدوار:** مدير النظام (Admin) حصرياً
* **المراجع:** متطلب `3.4.12`، مرحلة `8`، مخطط `Picture 6`
* **الحالة:** ⏸️ معلقة
* **بنود المراجعة والتحقق (Checklist):**
  - [ ] لوحة ضبط بوت تيليجرام (حقل الرمز Bot Token، وقائمة معرفات المحادثات Chat IDs).
  - [ ] لوحة ضبط مودم الـ GSM (إعدادات المنفذ، مؤشر الاتصال، وقائمة أرقام هواتف استلام الـ SMS للخطورة العالية).
  - [ ] جدول سجل إرسال التنبيهات مع شارة الحالة (ناجح / فاشل).
  - [ ] محاكاة ظهور تنبيه انقطاع الإنترنت وتنبيه تعذر الإرسال (`عرض تنبيه انقطاع الإنترنت` و `عرض رسالة فشل الإرسال`).

---

### [ ] المهمة 9: إعدادات النظام وسجل العمليات (`UI-AUDIT-09`)
* **الاسم في النظام:** `إعدادات النظام وسجل العمليات` (`System Settings & Audit Logs`)
* **الأدوار:** مدير النظام (Admin) حصرياً
* **المراجع:** متطلب `3.4.13`، مرحلة `9`، مخططات `Picture 1, 5`
* **الحالة:** ⏸️ معلقة
* **بنود المراجعة والتحقق (Checklist):**
  - [ ] جدول سجل الإجراءات (الوقت، اسم المستخدم، الدور، نوع العملية، التفاصيل).
  - [ ] فلاتر تصفية السجل حسب نوع العملية (تسجيل دخول، تعديل كاميرا، تصنيف إنذار خاطئ، تحديث حالة).
  - [ ] زر تصدير السجل.

---

## 📊 لوحة تقدم المهام الإجمالية
* إجمالي الواجهات: **9 واجهات**
* المنجز والمعتمد: **4 / 9** (44%) ✅
* المهمة النشطة حالياً: **المهمة 5: إدارة الكاميرات وتحديد ROI (`UI-CAMERA-05`)** ⏳
